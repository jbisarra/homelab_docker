###### NETWORKS

networks:
  default:
    driver: bridge
  socket_proxy:
    name: socket_proxy
    driver: bridge
    ipam:
      config:
        - subnet: 192.168.91.0/24

include:
  ###### SERVICES
  - shared/socket-proxy.yml
  - shared/crowdsec_agent.yml

    #Code Server/Infra  
  - compose/gitea.yml
