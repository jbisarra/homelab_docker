http:
  middlewares:
    chain-nextcloud:
      chain:
        middlewares:
          - middlewares-traefik-bouncer
          - middlewares-rate-limit
          - nextcloud-middlewares-secure-headers
          - nextcloud-redirect
